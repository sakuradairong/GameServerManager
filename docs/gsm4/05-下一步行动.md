# 下一步行动（方案冻结后）

本页是方案评审通过后的 **执行清单**，按顺序做。

## Step 1 — 冻结方案

- [x] 维护者确认 `docs/gsm4/` 决策无异议（分支策略、栈、迁移、里程碑）
- [x] 有争议项只改文档，不直接开码

## Step 2 — 创建 GSM4 实现线

- [x] 已创建 orphan `gsm4/main` 并推送基线（方案文档 + manifest）

## Step 3 — M0 工作分支

- [x] 分支 `cursor/gsm4-m0-scaffold-316b`（PR → `gsm4/main`）

## Step 4 — M0 最小文件集（建议）

```text
package.json                 # workspaces
apps/server/package.json
apps/server/src/index.ts     # Fastify + /api/v1/health + auth 雏形
apps/web/package.json
apps/web/src/main.tsx
packages/shared/src/index.ts
data/manifest.json
AGENTS.md
README.md
Dockerfile                   # 草稿即可
docs/                        # 同步方案文档
```

## Step 5 — 节奏

1. M0 可运行后，再开 M1（终端+实例）  
2. **M2 是架构成败点**：没有统一 DeploySession 之前，禁止平行抄 GSM3 多路由  
3. M3a（进行中）：能力表 UI + `bedrock` / `tmodloader`  
4. M3b+：`online`+sponsor、`factorio`、`cloud`、`mrpack`、Steam 更新/分支  
5. M6 前完成 migrator 与对照表勾选  

## Step 6 — 当前建议合并顺序

将已开 PR（M0→可用面板→SteamCMD 一键安装→M3a）按序合入 `gsm4/main`，避免长期仅堆草稿分支。

## 方案阶段本 PR 的范围

仅新增 `docs/gsm4/**` 到 **GSM3 `main`**，用于评审与对齐。  
**不包含** orphan 分支创建与业务代码（留待方案确认后的下一步）。
