# 下一步行动（方案冻结后）

本页是方案评审通过后的 **执行清单**，按顺序做。

## Step 1 — 冻结方案

- [x] 维护者确认 `docs/gsm4/` 决策无异议（分支策略、栈、迁移、里程碑）
- [x] 有争议项只改文档，不直接开码

## Step 2 — 创建 GSM4 实现线

- [x] 已创建 orphan `gsm4/main` 并推送基线（方案文档 + manifest）

## Step 3 — M0 工作分支

- [x] 分支 `cursor/gsm4-m0-scaffold-316b`（PR → `gsm4/main`）

## Step 4 — M0 最小文件集（建议）

```text
package.json                 # workspaces
apps/server/package.json
apps/server/src/index.ts     # Fastify + /api/v1/health + auth 雏形
apps/web/package.json
apps/web/src/main.tsx
packages/shared/src/index.ts
data/manifest.json
AGENTS.md
README.md
Dockerfile                   # 草稿即可
docs/                        # 同步方案文档
```

## Step 5 — 节奏

1. M0 可运行后，再开 M1（终端+实例）  
2. **M2 是架构成败点**：没有统一 DeploySession 之前，禁止平行抄 GSM3 多路由  
3. M3a（已合入）：能力表 UI + `bedrock` / `tmodloader`  
4. M3b+（跳过赞助）：`factorio`、`mrpack`、Steam 更新/分支
5. **暂缓**：赞助校验、`online` 在线部署  
6. M6 前完成 migrator 与对照表勾选  

## Step 6 — 合并状态

M0→M3 相关能力已进入 `gsm4/main`：统一部署内核、扩展执行器与 Steam 更新已交付；不可用的云构建能力已删除。

## Step 7 — 当前重点

1. 继续 M4 的备份、定时任务和环境管理；完成前不显示空导航入口。
2. 按 [06-安全与发布门禁](./06-安全与发布门禁.md) 保持认证、路径、上传、归档与实时通信边界。
3. M6 前完成 migrator、发布产物和功能对照表关闭。
